---
name: implement-issues
description: Orchestrate parallel implementation of every issue in a directory using subagents that each run /tdd in an isolated git worktree. Use when user wants to bulk-implement a backlog generated by /to-issues, run TDD across many tickets concurrently, asks for parallel issue implementation, or runs /implement-issues. Requires a path to the issues directory as an argument (e.g. `docs/ephemeral/<feature>/issues/`).
---

# Implement Issues (Parallel TDD)

Orchestrate parallel implementation of every issue in a given directory. Each issue is implemented by a fresh subagent that runs `/tdd` inside its own git worktree. The skill is complete only when **every** issue in the directory is `Status: done`.

This is the natural follow-up to `/to-issues`: that skill produces the backlog, this skill burns it down.

## Required argument

A path to an issues directory (e.g. `docs/ephemeral/unified-coverage/issues/`).

If no path is provided, **stop immediately** and reply:

> This skill requires a path to the issues directory (e.g. `docs/ephemeral/<feature>/issues/`). Re-run with the path as an argument.

Do not guess. Do not infer from recent activity. Do not pick "the most recent feature directory under `docs/ephemeral/`". Refuse and ask.

If the path does not exist, is not a directory, or contains no `*.md` issue files, stop and tell the user.

## Process

### 1. Survey the backlog

Read every `*.md` file under the given path. For each, capture:

- Absolute path
- `Status:` line — treat missing, empty, `todo`, `open`, `ready`, or `in-progress` as **not done**; treat `done`, `merged`, `closed`, or `shipped` as **done**
- `## Blocked by` block — references to other issues in the same directory (paths or `NN-slug` shorthands)

Build the dependency graph.

Print a summary:

- Total issues
- Already done
- Not done
- Issues with unresolvable blockers (typo, missing file, cycle) — flag these and stop until the user fixes them

### 2. Compute the next wave

A **wave** is the set of not-done issues whose blockers are all done.

Show the user the planned wave:

- Wave size (parallelism)
- Title and path of each issue in the wave

Quiz: _"Spawn N subagents in parallel for wave 1?"_ Wait for explicit approval before spawning. Do not assume yes.

### 3. Spawn the wave in parallel

In a **single message**, invoke one `Agent` tool call per issue in the wave so they run concurrently. For each:

- `subagent_type`: `general-purpose` unless the user named a coding-specific agent
- `isolation: "worktree"` — each subagent works in its own git worktree, so concurrent edits cannot collide on tracked files
- `description`: one-line task summary (e.g. _"Implement issue 03-checkout-flow"_)
- `prompt`: a self-contained brief — the subagent has no prior context, so spell everything out

Use this prompt template (substitute the issue's absolute path):

<subagent-prompt-template>
Implement the issue at `{{absolute-issue-path}}`.

**Read the issue file first.** It contains the acceptance criteria, the parent feature reference, and any dependency context. The file lives outside your git worktree (it's under `docs/ephemeral/`, which is gitignored), so read and write it via the absolute path I just gave you.

**Use loose TDD via the `/tdd` skill** — red-green-refactor, vertical slices only, one test → one implementation → repeat. No horizontal slicing (do not write all tests first). Cover happy case, unhappy case, and edge cases liberally.

**Project conventions:**

- Use the domain glossary in `CONTEXT.md` for naming
- Respect ADRs in `docs/adr/` that apply to the area you're touching
- Prettier formats all frontend code
- Atomic commits with short, expressive messages
- Push your branch to the remote without asking

**When you are done:**

- Update the `Status:` line in `{{absolute-issue-path}}` to `done` (write directly to the absolute path — do not commit it; the file is gitignored)
- Return a final message containing exactly:
  - `BRANCH: <branch-name>`
  - `COMMITS:` followed by a bullet list of commit subjects
  - `RESULT: done` — or `RESULT: blocked — <one-line reason>` if you cannot complete it (ambiguous spec, missing dependency, unresolved acceptance criterion)

Do not modify any other issue file. Do not close, rename, or modify the parent feature directory.
</subagent-prompt-template>

All wave subagents share one tool-call block — never spawn them serially.

### 4. Reconcile the wave

For each subagent that returned `RESULT: done`:

- Inspect its branch
- Merge or fast-forward it into the orchestrator's working branch — ask the user which strategy if the diff is non-trivial
- Verify the issue file's `Status:` is `done` in the orchestrator's working tree. The subagent writes directly to the absolute path (the file is gitignored, so worktrees can't carry the change), but verify and update the file yourself if the subagent missed it

For each subagent that returned `RESULT: blocked` or errored:

- Surface the reason to the user verbatim
- Do not mark the issue done
- Ask: retry, skip for now, or abort the orchestration

If a merge conflict arises during reconciliation, **stop** and escalate. Do not auto-resolve conflicts inside the orchestrator — the user should decide.

### 5. Loop until empty

Recompute the wave — newly-unblocked issues will appear. Repeat steps 2–4.

The skill is complete only when **every** issue file in the given path has `Status: done`. Print a final summary listing each issue and its final status.

## Anti-patterns

- **Do not** spawn subagents serially when they could run in parallel — they must share one tool-call block.
- **Do not** feed a subagent the whole backlog — each subagent owns exactly one issue.
- **Do not** guess at the issues directory if no argument was supplied.
- **Do not** mark `Status: done` unless the subagent explicitly returned `RESULT: done`.
- **Do not** continue past a wave if any subagent in that wave failed without user direction.
- **Do not** skip the wave-approval quiz, even on subsequent waves — the user may want to inspect the merged result before kicking off the next batch.

## Failure modes worth naming

- **Cyclic blockers** — stop, print the cycle, ask the user to fix the issue files.
- **Two issues in the same wave touch the same files** — merge conflict on reconcile is the symptom. The DAG should prevent this; if it doesn't, escalate so the user can re-author the blockers.
- **Subagent says it's done but tests are red on the merged branch** — surface this; the subagent's claim of "done" is not authoritative if CI disagrees. Treat as `RESULT: blocked` and ask the user.
